unbind C-b
set -g prefix C-Space
bind -n C-Space send-prefix

# Mouse mode
set -g mouse on

# split window replacement
unbind %
unbind '"'
bind v split-window 
bind g split-window -h

# vim-like screen focus
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
